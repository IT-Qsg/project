6379
hash:存储对象
    HMSET
    HGET
    HGETALL
list:消息推荐 消息队列
    LPUSH
    LRANGE
set:好友推荐（交集）
    sadd
    smembers
zset:权重
    zadd key score(可重复) value
    zrangebyscore key min max

SUBSCRIBE 频道
publish 将消息发布到指定频道

事物开启 mulit       没什么卵用，只是命令的集合
执行事物 exec

spring boot连接器 ：
 应用范围:
        在将请求发送到控制器之前
        在将响应发送给客户端之前
        调度 @EnableScheduling：执行特定时间段的任务